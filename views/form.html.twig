<!DOCTYPE html>
<html>
    <head>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <title>Find and Replace Words</title>
    </head>
    <body>
        <div class="container">
            <h1>Find and Replace Words</h1>
            <div class="form-group">
                <form action="/" method="post">
                    <label for="sentence">Enter a sentence:</label>
                    <input type="text" name="sentence" class="form-control">
                    <br>
                    <label for="word_to_search">Enter the word you want to replace:</label>
                    <input type="text" name="word_to_search" class="form-control">
                    <br>
                    <label for="replace_with">Enter the word you want it replaced with:</label>
                    <input type="text" name="replace_with" class="form-control">
                    <br>
                    <button type="submit" class="btn btn-info">Submit</button>
                </form>
            </div>

            <div class="result">
                {% if result is not empty %}
                        <h2>{{ result }}</h2>
                {% endif %}
            </div>
        </div>
    </body>
</html>
